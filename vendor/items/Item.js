class Item
{
	/**
	 * Abstract function equal.
	 * To verify that the current class matches the received.
	 * @param Item item
	 * @return boolean
	 */
	equal(item)
	{
		console.log("You need to create `equal` function in parent class.");
	}
}